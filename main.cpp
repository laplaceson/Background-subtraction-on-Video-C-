#include "function.h"
#include<string.h>
int main()
{
	CB();
	/*clock_t c1 = clock(), c2;
	vector<vector<objectuni>> tempg;
	int end = 0;
	bool flag = findxyandsort(tempg);
	if (flag)
	{
		end = compressy(tempg);
	}
	else { end = compress(tempg); }
	c2 = clock();
	cout << ((c2 - c1) / (double)(CLOCKS_PER_SEC));
	show(tempg,end);*/

	
	
	system("pause");
}